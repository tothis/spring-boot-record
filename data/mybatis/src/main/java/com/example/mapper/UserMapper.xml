<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserMapper">
    <!--
        传入参数时最好不写parameterType mybatis会自动处理传参 可直接使用属性值
        parameterMap和resultMap类似 设置入参字段映射(已弃用) https://mybatis.org/mybatis-3/sqlmap-xml.html
    -->
    <!--select id="findAll" resultMap="com.example.mapper.CoreMapper.user">
        SELECT
            a.id
            , a.user_name
            , a.password
            , a.age
            , a.address
            , b.id AS id1
            , b.detail
        FROM user a
        LEFT JOIN address b ON b.user_id = a.id
    </select-->

    <!-- <select id="findMap" resultMap="com.example.mapper.CoreMapper.user"> -->
    <select id="findMap" resultMap="user">
        SELECT id, user_name, password, age, mail, birthday, address FROM `user`
        <where>
            <if test="userName != null and userName != ''">
                AND user_name LIKE "%"#{userName}"%"
            </if>
            <if test="age != null and age != ''">
                AND age = #{age}
            </if>
        </where>
    </select>

    <select id="dbTree1" resultMap="com.example.mapper.CoreMapper.tree1">
        SELECT * FROM tree WHERE parent_id = #{parentId}
    </select>

    <select id="dbTree2" resultMap="com.example.mapper.GlobalMapper.tree2">
        SELECT * FROM tree WHERE parent_id = #{parentId}
    </select>

    <resultMap id="page-data" type="map"/>
    <resultMap id="page-count" type="long"/>
    <!--
        SQL_CALC_FOUND_ROWS 记录分页前数据数量
        FOUND_ROWS() 获取SQL_CALC_FOUND_ROWS记录的数据数量
        两个语句 但只执行一次主查询 有WHERE条件时FOUND_ROWS效率高 无WHERE条件时COUNT效率高
        需开启allowMultiQueries多语句执行 sql语句可携带分号
    -->
    <select id="findPage" resultMap="page-data,page-count">
        SELECT SQL_CALC_FOUND_ROWS * FROM user LIMIT 0, 2;
        SELECT FOUND_ROWS() `count`;
    </select>

    <resultMap id="one-user" type="user">
        <result column="detail" property="type.detail"/>
    </resultMap>

    <select id="user2" resultMap="one-user">
        SELECT a.user_name userName, b.detail FROM `user` a
        LEFT JOIN `type` b ON b.id = a.type_id
        WHERE a.id = 1
    </select>
</mapper>