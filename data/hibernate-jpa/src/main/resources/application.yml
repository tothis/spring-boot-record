spring:
  datasource:
    url: jdbc:mysql://192.168.10.200:3306/jpa?useUnicode=true&characterEncoding=utf8&autoReconnect=true&failOverReadOnly=false&useAffectedRows=true&serverTimezone=GMT&useSSL=false&zeroDateTimeBehavior=convertToNull
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
    # 设置创建表的引擎为InnoDB 方式一
    database-platform: org.hibernate.dialect.MySQL57InnoDBDialect
#    properties:
#      hibernate:
#        # MySQL55Dialect及其后续版本默认引擎为InnoDB 方式二
#        dialect: org.hibernate.dialect.MySQL57Dialect